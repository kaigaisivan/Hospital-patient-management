# Django settings
SECRET_KEY=czkzm&2pok8ls5v=hv_0uri!96=5%)50ntf@^fuxrh3sjlqsx#
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# Database (SQLite for local dev, Postgres for production)
# For local dev you can leave DATABASE_URL unset and Django will fallback to sqlite3
# If you want to test Postgres locally, uncomment and adjust:
# DATABASE_URL=postgres://user:password@localhost:5432/mydb

# Email settings (optional for local dev)
DJANGO_EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DJANGO_EMAIL_HOST=smtp.gmail.com
DJANGO_EMAIL_PORT=587
DJANGO_EMAIL_USE_TLS=True
DJANGO_EMAIL_HOST_USER=your_email@gmail.com
DJANGO_EMAIL_HOST_PASSWORD=your_app_password
DJANGO_DEFAULT_FROM_EMAIL=no-reply@hospitalcare.local
DJANGO_ADMINS=Admin:admin@example.com
